# Broad Example

Run:

```bash
agent-rules-kit init --preset broad --profile typescript --output .
```

This generates the broad compatibility set, with duplicate paths such as `AGENTS.md` rendered only once.
